import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.a77lic7ion.netconfiganalyzer',
  appName: 'NetConfig-Analyzer-AI',
  webDir: 'dist'
};

export default config;
